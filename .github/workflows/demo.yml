name: UCoffee Automated Tests

on:
  push:
    branches: [ "main" ]

 
jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Coffee
      run: echo "Hello, Ucoffee"
